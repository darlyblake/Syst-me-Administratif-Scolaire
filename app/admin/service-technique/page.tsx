"use client"

import { useEffect, useState } from "react"
import { LifeBuoy, Loader2, MessageSquare, Send, Wrench } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { supabaseBrowser } from "@/lib/supabase/client"

type Ticket = { id:string; establishment_id:string; establishment_name:string; establishment_code:string|null; subject:string; priority:string; status:string; created_at:string; updated_at:string; created_by:string }
type Msg = { id:string; sender_id:string; body:string; created_at:string; first_name:string|null; last_name:string|null; account_type:string }

const statusLabels: Record<string,string> = { open:"Ouvert", in_progress:"En cours", resolved:"Résolu", closed:"Fermé" }

export default function Page(){
 const [tickets,setTickets]=useState<Ticket[]>([]); const [selected,setSelected]=useState<Ticket|null>(null); const [messages,setMessages]=useState<Msg[]>([]); const [reply,setReply]=useState(""); const [loading,setLoading]=useState(true); const [sending,setSending]=useState(false); const [error,setError]=useState("")
 const load=async()=>{setLoading(true);setError("");const {data,error}=await supabaseBrowser.rpc("platform_admin_support_requests");if(error)setError(error.message);setTickets((data??[]) as Ticket[]);setLoading(false)}
 const loadMessages=async(id:string)=>{const {data,error}=await supabaseBrowser.rpc("get_support_messages",{p_request_id:id});if(error)setError(error.message);setMessages((data??[]) as Msg[])}
 useEffect(()=>{void load()},[])
 useEffect(()=>{if(selected)void loadMessages(selected.id)},[selected])
 const send=async()=>{if(!selected||!reply.trim())return;setSending(true);setError("");const {error}=await supabaseBrowser.rpc("reply_support_request",{p_request_id:selected.id,p_message:reply.trim()});if(error)setError(error.message);else{setReply("");await loadMessages(selected.id);await load()}setSending(false)}
 const setStatus=async(status:string)=>{if(!selected)return;const {error}=await supabaseBrowser.rpc("platform_admin_update_support_status",{p_request_id:selected.id,p_status:status});if(error)setError(error.message);else{await load();setSelected(tickets.find(t=>t.id===selected.id)??selected)}}
 return <div className="mx-auto max-w-7xl space-y-6"><div><p className="text-sm font-medium text-primary">Assistance</p><h1 className="text-3xl font-bold">Service technique</h1><p className="mt-1 text-muted-foreground">Recevez et traitez les demandes envoyées depuis les établissements.</p></div>{error&&<div className="rounded-lg border border-destructive/30 bg-destructive/5 px-4 py-3 text-sm text-destructive">{error}</div>}<div className="grid gap-6 lg:grid-cols-[380px_1fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><LifeBuoy className="h-5 w-5"/>Demandes</CardTitle></CardHeader><CardContent className="p-0">{loading?<div className="p-8 text-center"><Loader2 className="mx-auto h-5 w-5 animate-spin"/></div>:tickets.length===0?<div className="p-8 text-center text-sm text-muted-foreground">Aucune demande technique.</div>:<div className="divide-y">{tickets.map(t=><button key={t.id} type="button" onClick={()=>setSelected(t)} className={`w-full p-4 text-left hover:bg-muted/50 ${selected?.id===t.id?"bg-primary/[0.06]":""}`}><div className="flex items-start justify-between gap-2"><p className="font-medium">{t.subject}</p><span className="rounded-full bg-muted px-2 py-0.5 text-[11px]">{statusLabels[t.status]??t.status}</span></div><p className="mt-1 text-xs text-muted-foreground">{t.establishment_name} · {new Date(t.created_at).toLocaleDateString("fr-FR")}</p></button>)}</div>}</CardContent></Card><Card className="min-h-[520px]"><CardHeader><div className="flex flex-wrap items-center justify-between gap-3"><div><CardTitle>{selected?selected.subject:"Sélectionnez une demande"}</CardTitle>{selected&&<p className="mt-1 text-sm text-muted-foreground">{selected.establishment_name}{selected.establishment_code?` · ${selected.establishment_code}`:""}</p>}</div>{selected&&<select value={selected.status} onChange={e=>void setStatus(e.target.value)} className="h-9 rounded-md border bg-background px-3 text-sm"><option value="open">Ouvert</option><option value="in_progress">En cours</option><option value="resolved">Résolu</option><option value="closed">Fermé</option></select>}</div></CardHeader><CardContent>{!selected?<div className="flex min-h-[390px] items-center justify-center text-center text-muted-foreground"><div><Wrench className="mx-auto mb-3 h-10 w-10"/><p>Les messages techniques apparaîtront ici.</p></div></div>:<div className="flex min-h-[390px] flex-col"><div className="flex-1 space-y-3 overflow-y-auto pb-4">{messages.map(m=><div key={m.id} className="rounded-lg border p-3"><div className="flex items-center justify-between gap-3"><p className="text-sm font-medium">{[m.first_name,m.last_name].filter(Boolean).join(" ")||"Utilisateur"}</p><span className="text-xs text-muted-foreground">{new Date(m.created_at).toLocaleString("fr-FR")}</span></div><p className="mt-2 whitespace-pre-wrap text-sm">{m.body}</p></div>)}</div><div className="flex gap-2 border-t pt-4"><Input value={reply} onChange={e=>setReply(e.target.value)} placeholder="Répondre à l'établissement…" onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();void send()}}}/><Button onClick={()=>void send()} disabled={sending||!reply.trim()}>{sending?<Loader2 className="h-4 w-4 animate-spin"/>:<Send className="h-4 w-4"/>}</Button></div></div>}</CardContent></Card></div></div>
}
