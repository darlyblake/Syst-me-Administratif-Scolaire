// Temporary helper script kept outside the application runtime.
// The Settings pricing correction is applied by the deployment patch process.
export const settingsPricingCorrection = {
  inscription: "global",
  reinscription: "global",
  scolarite: "per_level",
};
